# Browser-visible API base URL. Use a same-origin path in development and deployment when a reverse proxy is available.
VITE_API_BASE_URL=/api

# Development-server proxy target. This value is used only by Vite's local server.
VITE_API_PROXY_TARGET=http://localhost:5249
